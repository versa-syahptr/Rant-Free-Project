apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: model-weights
  namespace: rfp
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
